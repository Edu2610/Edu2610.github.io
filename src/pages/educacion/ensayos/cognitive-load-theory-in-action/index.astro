---
import Layout from '../../../../layouts/Layout.astro';

// DEFINICIÓN DE CAPÍTULOS: CLT IN ACTION
const chapters = [
  {
    numero: "01",
    titulo: "Arquitectura Cognitiva: La Memoria de Trabajo",
    fecha: "Próximamente",
    desc: "Entendiendo el cuello de botella del aprendizaje: por qué nuestra RAM mental es limitada y cómo protegerla.",
    tags: ["Memoria de Trabajo", "Arquitectura Cognitiva"],
    link: "/educacion/ensayos/cognitive-load-theory-in-action/capitulo-1",
    active: true
  },
  
];
---

<Layout titulo="Serie: CLT in Action | Eduardo">
  
  <div class="background-grid"></div>

  <main class="container">
    
    <header class="page-header">
      <a href="/educacion/ensayos" class="btn-back">← Volver a Ensayos</a>
      
      <h1>Serie: <span class="text-gradient">CLT in Action</span></h1>
      <p class="subtitle">
        Optimizando el ancho de banda mental. Un análisis sobre cómo la Teoría de la Carga Cognitiva de Oliver Lovell redefine el diseño de la enseñanza.
      </p>
    </header>

    <section class="posts-grid">
      {chapters.map((chapter) => (
        <article class={`post-card ${!chapter.active ? 'inactive' : ''}`}>
          <a href={chapter.link} class="card-link">
            <div class="card-content">
              <div class="meta">
                <span class="date">{chapter.fecha}</span>
                <div class="tags">
                  {chapter.tags.map(tag => <span class="tag">#{tag}</span>)}
                </div>
              </div>
              
              <div class="chapter-number">CAPÍTULO {chapter.numero}</div>
              <h2>{chapter.titulo}</h2>
              <p>{chapter.desc}</p>
              
              {chapter.active ? (
                <span class="read-more">Leer capítulo →</span>
              ) : (
                <span class="read-more locked">En fase de estudio...</span>
              )}
            </div>
          </a>
        </article>
      ))}
    </section>

  </main>
</Layout>

<style>
  /* --- 1. CONFIGURACIÓN BASE --- */
  .background-grid {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -10;
    background-color: #0f172a;
    background-image: linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    mask-image: radial-gradient(circle at center, black 40%, transparent 100%);
  }

  .container {
    max-width: 900px;
    margin: 0 auto;
    padding: 4rem 1.5rem;
  }

  /* --- 2. HEADER --- */
  .page-header {
    margin-bottom: 5rem;
    text-align: left; 
  }

  .btn-back {
    display: inline-block;
    color: #94a3b8;
    text-decoration: none;
    font-size: 0.9rem;
    margin-bottom: 2rem;
    transition: color 0.2s;
  }
  .btn-back:hover { color: #4fd1c5; }

  h1 { font-size: 3rem; margin-bottom: 1rem; line-height: 1.1; color: #f1f5f9; }
  
  .subtitle { 
    font-size: 1.2rem; 
    color: #94a3b8; 
    max-width: 600px; 
    line-height: 1.6; 
  }

  .text-gradient {
    background: linear-gradient(45deg, #c084fc, #4fd1c5);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent;
  }

  /* --- 3. GRID DE CAPÍTULOS --- */
  .posts-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .post-card {
    position: relative;
  }

  .card-link {
    display: block;
    background: rgba(15, 23, 42, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 2.5rem;
    text-decoration: none;
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
  }

  .card-link:hover {
    border-color: #c084fc;
    background: rgba(15, 23, 42, 0.9);
    transform: translateY(-5px);
    box-shadow: 0 10px 30px -10px rgba(192, 132, 252, 0.15);
  }

  .post-card.inactive .card-link {
    opacity: 0.5;
    pointer-events: none;
    border-color: rgba(255, 255, 255, 0.05);
    filter: grayscale(0.5);
  }
  
  .chapter-number {
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 2px;
    color: #4fd1c5;
    margin-bottom: 0.5rem;
    margin-top: 1rem;
    text-transform: uppercase;
  }

  .card-content h2 {
    color: #f1f5f9;
    font-size: 1.8rem;
    margin: 0.5rem 0 1rem 0;
  }

  .card-content p {
    color: #94a3b8;
    line-height: 1.7;
    margin-bottom: 2rem;
  }

  .meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.85rem;
    color: #64748b;
  }
  
  .tags { display: flex; gap: 0.5rem; }
  
  .tag {
    background: rgba(192, 132, 252, 0.1);
    color: #c084fc;
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    font-weight: 500;
  }

  .read-more {
    color: #4fd1c5;
    font-weight: 600;
    font-size: 0.95rem;
  }
  
  .read-more.locked {
    color: #64748b;
    font-style: italic;
  }
</style>