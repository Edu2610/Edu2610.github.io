---
import Layout from '../../layouts/Layout.astro';
import Card from '../../components/Card.astro';

// AQUÍ DEFINIMOS TUS FUTUROS ARTÍCULOS
// Por ahora son ideas de lo que podrías escribir
const articulos = [
  {
    titulo: "¿Por qué odiamos las matemáticas?",
    desc: "Un análisis filosófico sobre la ansiedad numérica y cómo el sistema educativo falla en enseñar lógica.",
    link: "#" // Todavía no existe
  },
  {
    titulo: "La Ética en la Docencia",
    desc: "Reflexiones sobre la responsabilidad moral de formar a las nuevas generaciones.",
    link: "#"
  }
];

// AQUÍ TUS RECURSOS DESCARGABLES
const recursos = [
  {
    titulo: "Guía: Lógica Proposicional",
    desc: "PDF con ejercicios resueltos y explicaciones paso a paso. Nivel: Introductorio.",
    link: "#"
  },
  {
    titulo: "Cheat Sheet: Derivadas",
    desc: "Formulario rápido para estudiantes de cálculo diferencial.",
    link: "#"
  }
];
---

<Layout titulo="Educación | Eduardo">
  
  <section class="intro">
    <h1>Educación & <span class="text-gradient">Pensamiento</span></h1>
    <p>
      La enseñanza no es solo transmitir datos, es <strong>encender una llama</strong>. 
      Aquí comparto mi visión pedagógica, recursos didácticos y ensayos sobre la intersección entre filosofía y matemáticas.
    </p>
  </section>

  <section class="methodology">
    <div class="method-card">
      <h2>🧠 Mi Metodología</h2>
      <p>
        "No busco que memorices fórmulas, busco que entiendas el lenguaje del universo."
      </p>
      <ul class="principles">
        <li>✨ <strong>Lógica sobre Memoria:</strong> Entender el "por qué" antes que el "cómo".</li>
        <li>🤝 <strong>Empatía Cognitiva:</strong> Adaptar la explicación a la mente del estudiante.</li>
        <li>🏛️ <strong>Base Filosófica:</strong> Las matemáticas como herramienta de pensamiento crítico.</li>
      </ul>
    </div>
  </section>

  <section class="content-section">
    <h2 class="section-title">🖋️ Ensayos y Opinión</h2>
    <p class="subtitle">Reflexiones sobre el sistema educativo y la filosofía.</p>
    
    <div class="grid-content">
      {articulos.map((item) => (
        <Card 
          titulo={item.titulo}
          descripcion={item.desc}
          enlace={item.link}
        />
      ))}
    </div>
  </section>

  <section class="content-section">
    <h2 class="section-title">📚 Caja de Herramientas</h2>
    <p class="subtitle">Recursos gratuitos creados para mis estudiantes.</p>
    
    <div class="grid-content">
      {recursos.map((item) => (
        <Card 
          titulo={item.titulo}
          descripcion={item.desc}
          enlace={item.link}
        />
      ))}
    </div>
  </section>

</Layout>

<style>
  /* ESTILOS ESPECÍFICOS DE EDUCACIÓN */
  
  /* Usamos el Violeta como color principal en esta página */
  .text-gradient {
    background: linear-gradient(45deg, var(--accent-purple), var(--accent-mint));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .intro {
    margin-bottom: 4rem;
    
    /* Agregamos esto: */
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .intro p { 
    color: var(--text-muted); 
    font-size: 1.1rem; 
    max-width: 800px; 
    line-height: 1.6; 
  }

  /* También centramos los títulos de las secciones para consistencia */
  .section-title { 
    font-size: 1.5rem; 
    color: var(--text-main); 
    margin-bottom: 0.5rem;
    text-align: center; /* <--- Nuevo */
  }
  
  .subtitle { 
    color: var(--text-muted); 
    margin-bottom: 2rem; 
    margin-top: 0; 
    font-size: 0.95rem; 
    text-align: center; /* <--- Nuevo */
  }
  .intro strong { color: var(--accent-purple); }

  /* TARJETA DE METODOLOGÍA (Destacada) */
  .methodology { margin-bottom: 4rem; }
  
  .method-card {
    background: linear-gradient(180deg, rgba(192, 132, 252, 0.1) 0%, rgba(15, 23, 42, 0) 100%);
    border: 1px solid rgba(192, 132, 252, 0.2);
    border-radius: var(--radius-md);
    padding: 2.5rem;
  }

  .method-card h2 { color: var(--accent-purple); font-size: 1.8rem; margin-top: 0; }
  .method-card p { font-size: 1.2rem; font-style: italic; color: var(--text-main); margin-bottom: 2rem; }

  .principles {
    list-style: none;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }

  .principles li {
    background: rgba(15, 23, 42, 0.5);
    padding: 1rem;
    border-radius: 8px;
    border-left: 3px solid var(--accent-purple);
    color: var(--text-muted);
  }

  .principles strong { color: var(--text-main); display: block; margin-bottom: 0.3rem; }

  /* SECCIONES DE CONTENIDO */
  .content-section { margin-bottom: 4rem; }
  .section-title { font-size: 1.5rem; color: var(--text-main); margin-bottom: 0.5rem; }
  .subtitle { color: var(--text-muted); margin-bottom: 2rem; margin-top: 0; font-size: 0.95rem; }

  .grid-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));