---
import Layout from '../../../../layouts/Layout.astro';

const recursos = [
  {
    nombre: "Planificación Clase 3: Modelamiento",
    formato: "PDF",
    desc: "Guía docente detallada con la dinámica de 'Misiones' y gestión del aula + anexo.",
    link: "/docs/planificacion_1ro_medio/1ro_medio_operatoria_fracciones_cierresuma.pdf", 
    icon: "📋"
  },
];
---

<Layout titulo="Modelamiento y Contexto | Primero Medio">
  <div class="background-grid"></div>

  <main class="container">
    <header class="page-header">
      <a href="/educacion/recursos/primero-medio" class="btn-back">← Volver a la Unidad</a>
      <h1>Desafíos de <span class="text-gradient">Ingeniería</span></h1>
      <p class="subtitle">Clase 3: Modelamiento de situaciones reales y toma de decisiones técnicas.</p>
    </header>

    <section class="content-section">
      <div class="glass-card">
        <h2>La Consultora Técnica</h2>
        <p>
          Hoy el aula se transforma en una firma de auditoría. La premisa es simple: <strong>"El decimal miente, la fracción dice la verdad"</strong>. 
          Los estudiantes enfrentan casos multidisciplinarios (Finanzas, Derecho, Química) donde el uso de aproximaciones decimales ha causado desastres. 
          Su misión es recalcular todo usando fracciones exactas para recuperar dinero perdido o evitar fallos estructurales.
        </p>
      </div>
    </section>

    <section class="content-section">
      <h2>Casos de Estudio</h2>
      <div class="concepts-grid">
        <div class="concept-card" style="border-top: 3px solid #10b981;">
          <h3>Finanzas y Legal</h3>
          <p><strong>Auditoría Forense.</strong> Detección de fraudes por redondeo y validación de testamentos imposibles ($>1$).</p>
        </div>
        <div class="concept-card" style="border-top: 3px solid #3b82f6;">
          <h3>Ingeniería Química</h3>
          <p><strong>Mezclas Exactas.</strong> Cálculo de niveles en estanques considerando evaporación (pérdida) y recargas.</p>
        </div>
        <div class="concept-card" style="border-top: 3px solid #f59e0b;">
          <h3>Logística y Obra</h3>
          <p><strong>Carga Crítica.</strong> Ajuste de tuberías y control de peso en puentes. ¿Soporta la estructura o colapsa?</p>
        </div>
      </div>
    </section>

    <section class="content-section">
      <div class="glass-card warning-card" style="margin-top: 3rem;">
        <h3>⚠️ Precisión Absoluta</h3>
        <p>
          En esta clase, un error de $0,01$ no es "un poquito". En ingeniería, es la diferencia entre un puente que resiste y uno que cae.
          Los estudiantes deben justificar técnicamente sus decisiones: <em>"El testamento es inválido porque la suma de las partes supera al entero"</em>.
        </p>
      </div>
    </section>

    <section class="content-section">
      <h2>Descargas Docentes</h2>
      <div class="download-grid">
        {recursos.map(item => (
          <div class="download-card">
            <div class="card-info">
              <span class="file-icon">{item.icon}</span>
              <div>
                <h3>{item.nombre}</h3>
                <span class="file-type">{item.formato}</span>
                <p style="padding-right: 1rem;">{item.desc}</p>
              </div>
            </div>
            <a href={item.link} download class="download-btn">Descargar</a>
          </div>
        ))}
      </div>
    </section>
  </main>
</Layout>

<style>
  /* --- ESTILOS ESPECÍFICOS --- */
  .container { max-width: 900px; margin: 0 auto; padding: 4rem 1.5rem; }
  
  /* Gradiente Verde/Azul (Profesional/Dinero) para esta clase */
  .text-gradient { background: linear-gradient(to right, #10b981, #3b82f6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  
  .glass-card, .concept-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2rem; border-radius: 16px; color: #94a3b8; line-height: 1.7;
    backdrop-filter: blur(10px);
  }

  .warning-card { border-left: 4px solid #10b981; background: rgba(16, 185, 129, 0.05); }
  .warning-card h3 { color: #10b981; margin-bottom: 0.5rem; }

  /* Grid de Conceptos */
  .concepts-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1.5rem; margin-top: 1rem; }
  .concept-card h3 { color: #e2e8f0; margin-bottom: 0.5rem; font-family: var(--font-heading); font-size: 1.5rem; }
  
  /* Botones y Headers */
  .page-header { margin-bottom: 3rem; }
  .btn-back { color: var(--text-muted); text-decoration: none; display: block; margin-bottom: 1rem; }
  h1 { font-family: var(--font-heading); font-size: 3rem; color: #f8fafc; }
  h2 { color: #f1f5f9; font-size: 1.8rem; margin-bottom: 1.5rem; margin-top: 3rem; }
  
  /* --- BOTÓN DE DESCARGA --- */
  .download-grid { display: grid; gap: 1.5rem; }
  .download-card { 
    display: flex; 
    justify-content: space-between; 
    align-items: center; 
    background: rgba(30, 41, 59, 0.5); 
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2rem; 
    border-radius: 16px; 
    backdrop-filter: blur(10px);
    gap: 2rem;
  }

  .card-info { 
    display: flex; 
    gap: 1.5rem; 
    align-items: center; 
    flex: 1; 
  }

  .file-icon { font-size: 2.5rem; flex-shrink: 0; }
  
  .download-btn { 
    flex-shrink: 0; 
    background: #10b981; /* Color Esmeralda */
    color: #0f172a; 
    padding: 0.8rem 1.5rem; 
    border-radius: 8px; 
    text-decoration: none; 
    font-weight: bold; 
    white-space: nowrap; 
    transition: transform 0.2s;
  }
  .download-btn:hover { transform: scale(1.05); }
  
  @media (max-width: 768px) { 
    .download-card { flex-direction: column; align-items: stretch; gap: 1.5rem; text-align: center; } 
    .card-info { flex-direction: column; }
    .download-btn { text-align: center; }
  }
</style>