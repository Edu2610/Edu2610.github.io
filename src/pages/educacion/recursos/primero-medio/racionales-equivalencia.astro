---
import Layout from '../../../../layouts/Layout.astro';

// Datos de los archivos para facilitar el mantenimiento
const recursos = [
  {
    nombre: "Planificación + Set de Tarjetas",
    formato: "PDF Único",
    desc: "Guía docente completa que incluye el análisis didáctico y el set de 23 pares de equivalencias para recortar (páginas finales).",
    link: "/docs/planificacion-racionales.pdf",
    icon: "📚"
  }
];
---

<Layout titulo="Racionales: Equivalencia | Primero Medio">
  <div class="background-grid"></div>

  <main class="container">
    <header class="page-header">
      <a href="/educacion/recursos/primero-medio" class="btn-back">← Volver al Nivel</a>
      <h1>Racionales: <span class="text-gradient">Equivalencia y Densidad</span></h1>
      <p class="subtitle">Clase 1: Transición de los Enteros a los Racionales.</p>
    </header>

    <section class="content-section">
      <div class="glass-card">
        <h2>Propuesta Didáctica</h2>
        <p>
          Esta secuencia aborda el conjunto de los Números Racionales (ℚ) como una evolución necesaria de los Enteros. 
          El foco está en la <strong>equivalencia semántica</strong>: entender que la fracción y el decimal son simplemente dos lenguajes para un mismo punto en la recta.
        </p>
      </div>
    </section>

    <section class="content-section">
      <h2>Demostración Dinámica</h2>
      <div class="visual-container">
        <img
          src="/img/densidad-racionales.gif" 
          alt="Animación de GeoGebra mostrando la densidad de los racionales" 
          class="geogebra-gif"
        />
        <div class="visual-caption">
          <p><strong>Visualizador de igualdad:</strong> Esta animación demuestra como con dos notaciones hacemos referencia al mismo valor.</p>
          <a href="https://www.geogebra.org/m/ku27b4rx" target="_blank" class="external-link">Ver Applet interactivo en GeoGebra ↗</a>
        </div>
      </div>
    </section>

    <section class="content-section">
      <h2>Material de Apoyo Consolidado</h2>
      <div class="download-grid">
        {recursos.map(item => (
          <div class="download-card">
            <div class="card-info">
              <span class="file-icon">{item.icon}</span>
              <div>
                <h3>{item.nombre}</h3>
                <span class="file-type">{item.formato}</span>
                <p>{item.desc}</p>
              </div>
            </div>
            <a href={item.link} download class="download-btn">Descargar Material Completo</a>
          </div>
        ))}
      </div>
    </section>
  </main>
</Layout>

<style>
  /* Reutilizamos variables y estilos base de tu portafolio */
  .container { max-width: 900px; margin: 0 auto; padding: 4rem 1.5rem; }
  .content-section { margin-bottom: 4rem; }
  
  h2 { color: #f1f5f9; font-size: 1.8rem; margin-bottom: 1.5rem; }

  /* Glass Card Estilo Portafolio */
  .glass-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 2rem;
    border-radius: 16px;
    color: #94a3b8;
    line-height: 1.7;
  }

  /* Contenedor del GIF */
  .visual-container {
    background: #000;
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  .geogebra-gif { width: 100%; height: auto; display: block; }
  .visual-caption { padding: 1.5rem; background: rgba(15, 23, 42, 0.9); border-top: 1px solid rgba(255,255,255,0.05); }
  .visual-caption p { color: #94a3b8; margin-bottom: 1rem; }
  .external-link { color: var(--accent-mint); text-decoration: none; font-weight: 600; }

  /* Grid de Descargas */
  .download-grid { display: grid; gap: 1.5rem; }
  .download-card {
    display: flex; justify-content: space-between; align-items: center;
    background: rgba(30, 41, 59, 0.5);
    padding: 1.5rem;
    border-radius: 12px;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }
  .card-info { display: flex; gap: 1.5rem; align-items: flex-start; }
  .file-icon { font-size: 2.5rem; }
  .file-type { font-size: 0.7rem; background: rgba(255,255,255,0.1); padding: 2px 8px; border-radius: 4px; color: #fff; text-transform: uppercase; }
  .card-info h3 { margin: 0.5rem 0; color: #fff; }
  .card-info p { font-size: 0.9rem; color: #94a3b8; margin: 0; }

  .download-btn {
    background: var(--accent-mint);
    color: #0f172a;
    padding: 0.8rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: bold;
    transition: transform 0.2s;
  }
  .download-btn:hover { transform: scale(1.05); }

  @media (max-width: 600px) {
    .download-card { flex-direction: column; gap: 1.5rem; align-items: flex-start; }
  }
</style>