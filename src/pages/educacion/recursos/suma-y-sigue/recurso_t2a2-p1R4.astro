---
import Layout from '../../../../layouts/Layout.astro';
// Asegúrate de que la ruta al Layout sea la correcta según tu estructura
---

<Layout titulo="Covariación No Proporcional">
    <div class="recurso-wrapper">
        <div class="card">
            <div class="header-text">
                
            </div>

            <div class="interactive-container">
                
                <svg class="svg-layer svg-top" viewBox="0 0 700 40">
                    <defs>
                        <marker id="arrow-blue" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
                            <path d="M 0 0 L 10 5 L 0 10 z" fill="#2563eb" />
                        </marker>
                    </defs>
                    <path d="M 234 35 Q 287 -10 341 35" fill="none" stroke="#2563eb" stroke-width="2" marker-end="url(#arrow-blue)" />
                    <path d="M 371 35 Q 425 -10 478 35" fill="none" stroke="#2563eb" stroke-width="2" marker-end="url(#arrow-blue)" />
                    <path d="M 509 35 Q 563 -10 616 35" fill="none" stroke="#2563eb" stroke-width="2" marker-end="url(#arrow-blue)" />
                </svg>

                <input type="text" id="top1" class="jump-input input-blue" style="left: 287px; top: -55px;" autocomplete="off" inputmode="text" pattern="[0-9+\-]*">
                <input type="text" id="top2" class="jump-input input-blue" style="left: 425px; top: -55px;" autocomplete="off" inputmode="text" pattern="[0-9+\-]*">
                <input type="text" id="top3" class="jump-input input-blue" style="left: 563px; top: -55px;" autocomplete="off" inputmode="text" pattern="[0-9+\-]*">

                <table>
                    <tr>
                        <td class="col-header">x: Medida del lado (cm)</td>
                        <td>1</td>
                        <td>2</td>
                        <td>3</td>
                        <td>4</td>
                    </tr>
                    <tr>
                        <td class="col-header">y: Área del cuadrado (cm²)</td>
                        <td>1</td>
                        <td>4</td>
                        <td>9</td>
                        <td>16</td>
                    </tr>
                </table>

                <svg class="svg-layer svg-bottom" viewBox="0 0 700 40">
                    <defs>
                        <marker id="arrow-red" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
                            <path d="M 0 0 L 10 5 L 0 10 z" fill="#d32f2f" />
                        </marker>
                    </defs>
                    <path d="M 234 5 Q 287 50 341 5" fill="none" stroke="#d32f2f" stroke-width="2" marker-end="url(#arrow-red)" />
                    <path d="M 371 5 Q 425 50 478 5" fill="none" stroke="#d32f2f" stroke-width="2" marker-end="url(#arrow-red)" />
                    <path d="M 509 5 Q 563 50 616 5" fill="none" stroke="#d32f2f" stroke-width="2" marker-end="url(#arrow-red)" />
                </svg>

                <input type="text" id="bot1" class="jump-input input-red" style="left: 287px; bottom: -85px;" autocomplete="off" inputmode="text" pattern="[0-9+\-]*">
                <input type="text" id="bot2" class="jump-input input-red" style="left: 425px; bottom: -85px;" autocomplete="off" inputmode="text" pattern="[0-9+\-]*">
                <input type="text" id="bot3" class="jump-input input-red" style="left: 563px; bottom: -85px;" autocomplete="off" inputmode="text" pattern="[0-9+\-]*">

            </div>

            <button id="btn-verify" class="btn-verify">Verificar Respuesta</button>
            <div id="feedback" class="feedback-text"></div>
        </div>
    </div>
</Layout>

<style>
    .recurso-wrapper {
        font-family: 'Inter', system-ui, sans-serif;
        background: #f8fafc;
        display: flex;
        justify-content: center;
        padding: 40px 20px;
        width: 100%;
    }

    .card {
        background: white;
        padding: 40px 20px;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        max-width: 800px;
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .header-text { text-align: center; margin-bottom: 20px; }
    .header-text p.title { color: #0f172a; margin: 0 0 5px 0; font-size: 1.25rem; font-weight: 700; }
    .header-text p.desc { color: #64748b; font-size: 0.95rem; margin: 0; }

    .interactive-container {
        width: 700px;
        position: relative;
        margin-top: 70px; 
        margin-bottom: 70px; 
    }

    table {
        width: 100%;
        border-collapse: collapse;
        table-layout: fixed;
        position: relative;
        z-index: 1;
    }

    td {
        border: 1px solid #475569;
        height: 60px;
        text-align: center;
        vertical-align: middle;
        font-size: 1.2rem;
        font-weight: 600;
        color: #1e293b;
    }

    .col-header {
        width: 180px;
        background-color: #f1f5f9;
        font-weight: 700;
        text-align: right;
        padding-right: 15px;
    }

    .jump-input {
        position: absolute;
        width: 44px;
        height: 28px;
        font-family: 'Inter', sans-serif;
        font-size: 1rem;
        font-weight: 700;
        text-align: center;
        border: 2px solid #cbd5e1;
        border-radius: 6px;
        background: #ffffff;
        transition: all 0.2s;
        outline: none;
        z-index: 10;
        transform: translate(-50%, -50%); 
    }

    .jump-input:focus { box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.15); }
    .input-blue { color: #2563eb; }
    .input-blue:focus { border-color: #2563eb; }
    .input-red { color: #d32f2f; }
    .input-red:focus { border-color: #d32f2f; }

    .correct { background-color: #ecfdf5 !important; border-color: #10b981 !important; color: #047857 !important; }
    .incorrect { background-color: #fef2f2 !important; border-color: #ef4444 !important; color: #b91c1c !important; }

    .btn-verify {
        padding: 12px 30px;
        margin-top: 10px;
        border: none;
        border-radius: 8px;
        background: #0f172a;
        color: white;
        font-size: 1rem;
        font-weight: 700;
        cursor: pointer;
        transition: background 0.2s, transform 0.2s;
    }

    .btn-verify:hover:not(:disabled) { background: #334155; transform: translateY(-2px); }
    .btn-verify:disabled { background: #cbd5e1; cursor: not-allowed; color: #f8fafc; }

    .feedback-text {
        margin-top: 15px;
        font-size: 0.95rem;
        font-weight: 400;
        line-height: 1.5;
        min-height: 20px;
        text-align: center;
        max-width: 600px;
    }

    .svg-layer {
        position: absolute;
        left: 0;
        width: 100%;
        pointer-events: none; 
    }
    .svg-top { top: -40px; height: 40px; }
    .svg-bottom { bottom: -40px; height: 40px; }
</style>

<script>
    const setupVariacionNoProporcional = () => {
        const correctAnswers: Record<string, string> = {
            top1: "+1", top2: "+1", top3: "+1",
            bot1: "+3", bot2: "+5", bot3: "+7"
        };

        let estadoSaltos: Record<string, string> = {
            top1: "", top2: "", top3: "",
            bot1: "", bot2: "", bot3: ""
        };

        const btnVerify = document.getElementById('btn-verify') as HTMLButtonElement | null;
        const feedback = document.getElementById('feedback') as HTMLElement | null;
        const inputIds = Object.keys(correctAnswers);

        if (!btnVerify || !feedback) return;

        function formatAnswer(val: string, expected: string) {
            let clean = val.trim();
            // Perdona si el alumno omite el signo "+"
            if (expected.startsWith("+") && clean === expected.substring(1)) return expected;
            return clean;
        }

        inputIds.forEach(id => {
            const input = document.getElementById(id) as HTMLInputElement | null;
            if (!input) return;

            // Clonar nodo para resetear event listeners en Astro View Transitions
            const newClone = input.cloneNode(true) as HTMLInputElement;
            input.parentNode?.replaceChild(newClone, input);

            newClone.addEventListener('input', (e) => {
                estadoSaltos[id] = (e.target as HTMLInputElement).value;
            });
        });

        const newBtnVerify = btnVerify.cloneNode(true) as HTMLButtonElement;
        btnVerify.parentNode?.replaceChild(newBtnVerify, btnVerify);

        newBtnVerify.addEventListener('click', () => {
            let correctCount = 0;

            inputIds.forEach(id => {
                const input = document.getElementById(id) as HTMLInputElement | null;
                if (!input) return;

                const userVal = formatAnswer(estadoSaltos[id], correctAnswers[id]);
                const correctVal = correctAnswers[id];

                input.disabled = true;

                if (userVal === correctVal) {
                    input.classList.add('correct');
                    input.value = correctVal; // Forza el formato correcto (+N)
                    correctCount++;
                } else {
                    input.classList.add('incorrect');
                    input.value = correctVal; 
                }
            });

            if (correctCount === inputIds.length) {
                feedback.style.color = "#047857";
                feedback.innerHTML = "¡Excelente! Como puedes ver, aunque el lado aumenta de manera constante (+1), el área crece cada vez más rápido (+3, +5, +7). <br><strong>¡No toda variación es proporcional!</strong>";
            } else {
                feedback.style.color = "#b91c1c";
                feedback.textContent = "Hay errores en el cálculo de las diferencias. Recuerda restar la celda actual menos la celda anterior.";
            }

            newBtnVerify.disabled = true;
        });

        // ==========================================
        // INTEGRACIÓN EDX (JSInput API)
        // ==========================================
        (window as any).getGrade = () => JSON.stringify(estadoSaltos);
        (window as any).getState = () => JSON.stringify(estadoSaltos);
        (window as any).setState = (stateStr: string) => {
            if (!stateStr) return;
            try {
                estadoSaltos = JSON.parse(stateStr);
                inputIds.forEach(id => {
                    if (estadoSaltos[id] !== "") {
                        const input = document.getElementById(id) as HTMLInputElement | null;
                        if (input) input.value = estadoSaltos[id];
                    }
                });
            } catch(e) {
                console.error("Error restaurando estado en edX", e);
            }
        };
    };

    document.addEventListener('DOMContentLoaded', setupVariacionNoProporcional);
    document.addEventListener('astro:page-load', setupVariacionNoProporcional);
</script>